import Test.Hspec
import Lib

import Data.Time

-- main :: IO ()
-- main = hspec $ do
    -- describe "Gets current time" $ do
        -- it "Should get current time" $ do
            -- getTime `shouldBe` getCurrentTime 
